import React from "react";
import {LoadingBar, LoadingWrapper} from "./style";

const Loading = () => {
	return (
		<LoadingWrapper>
			<LoadingBar/>
		</LoadingWrapper>
	);
};

export default Loading;